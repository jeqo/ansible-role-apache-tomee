---
# tasks file for apache-tomee
- include: prepare-os.yml
  tags:
    - preinstall

- include: set-vars.yml
  tags:
    - install

- include: install-tomee.yml
  tags:
    - install

- include: configure-tomee.yml
  tags:
    - configure
